<?xml version="1.0" encoding="utf-8"?>
<!--
  Map destination picker.

  The map fills everything ABOVE the bottom card rather than sliding under it, so the centre of the
  map is the centre of what the user can actually see — the pin used to sit low, half-hidden behind an
  opaque panel. The card keeps only what is always needed (where, how far, confirm) and hides the
  alert-ring controls behind one tap, so the map gets the screen.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface"
    android:orientation="vertical">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <org.osmdroid.views.MapView
            android:id="@+id/map"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <!-- Expanding ring behind the pin, pulsed when a place is chosen so the pick is unmissable. -->
        <View
            android:id="@+id/pin_pulse"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="center"
            android:alpha="0"
            android:background="@drawable/bg_pin_pulse" />

        <!-- The exact point being picked. The pin above marks it; this dot IS it. -->
        <View
            android:layout_width="10dp"
            android:layout_height="10dp"
            android:layout_gravity="center"
            android:background="@drawable/bg_pin_anchor" />

        <!-- Offset up by half its height so the teardrop's TIP lands on the anchor dot. -->
        <ImageView
            android:id="@+id/pin"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_gravity="center"
            android:contentDescription="@string/map_pick_hint"
            android:src="@drawable/ic_place"
            android:translationY="-22dp"
            app:tint="?attr/colorError" />

        <!-- Search bar + results, floating over the map. -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="top"
            android:fitsSystemWindows="true"
            android:orientation="vertical"
            android:paddingStart="12dp"
            android:paddingTop="8dp"
            android:paddingEnd="12dp">

            <com.google.android.material.card.MaterialCardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:cardCornerRadius="28dp"
                app:cardElevation="6dp">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:gravity="center_vertical"
                    android:orientation="horizontal"
                    android:paddingStart="4dp"
                    android:paddingEnd="4dp">

                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/back_button"
                        style="@style/Widget.Material3.Button.IconButton"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:contentDescription="@string/map_back"
                        app:icon="@drawable/ic_arrow_back"
                        app:iconTint="?attr/colorOnSurfaceVariant" />

                    <EditText
                        android:id="@+id/search_input"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:background="@null"
                        android:hint="@string/map_search_hint"
                        android:imeOptions="actionSearch"
                        android:importantForAutofill="no"
                        android:inputType="text"
                        android:maxLines="1"
                        android:minHeight="@dimen/touch_target_min"
                        android:paddingVertical="12dp"
                        android:textColor="?attr/colorOnSurface"
                        android:textColorHint="?attr/colorOnSurfaceVariant" />

                    <ProgressBar
                        android:id="@+id/search_progress"
                        style="?android:attr/progressBarStyleSmall"
                        android:layout_width="24dp"
                        android:layout_height="24dp"
                        android:layout_marginEnd="8dp"
                        android:visibility="gone"
                        tools:visibility="visible" />

                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/clear_button"
                        style="@style/Widget.Material3.Button.IconButton"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:contentDescription="@string/map_search_clear"
                        android:visibility="gone"
                        app:icon="@drawable/ic_close"
                        app:iconTint="?attr/colorOnSurfaceVariant"
                        tools:visibility="visible" />
                </LinearLayout>
            </com.google.android.material.card.MaterialCardView>

            <com.google.android.material.card.MaterialCardView
                android:id="@+id/search_results_card"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:visibility="gone"
                app:cardCornerRadius="20dp"
                app:cardElevation="6dp"
                tools:visibility="visible">

                <androidx.core.widget.NestedScrollView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:maxHeight="300dp">

                    <LinearLayout
                        android:id="@+id/search_results"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="vertical"
                        android:paddingVertical="4dp" />
                </androidx.core.widget.NestedScrollView>
            </com.google.android.material.card.MaterialCardView>
        </LinearLayout>

        <!-- Map controls, thumb-side and out of the way: saved places, compass, recentre. -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="16dp"
            android:orientation="vertical">

            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/fab_favorites"
                style="?attr/floatingActionButtonSmallStyle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="12dp"
                android:contentDescription="@string/map_favorites"
                android:src="@drawable/ic_star"
                app:backgroundTint="?attr/colorSurfaceContainerHighest"
                app:elevation="4dp"
                app:shapeAppearanceOverlay="@style/ShapeAppearance.AlarmTracker.Circle"
                app:tint="?attr/colorOnSurfaceVariant" />

            <!-- Points to north; tap to face north up, or to follow the way the phone is pointing. -->
            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/fab_compass"
                style="?attr/floatingActionButtonSmallStyle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="12dp"
                android:contentDescription="@string/map_compass"
                android:src="@drawable/ic_compass"
                app:backgroundTint="?attr/colorSurfaceContainerHighest"
                app:elevation="4dp"
                app:shapeAppearanceOverlay="@style/ShapeAppearance.AlarmTracker.Circle"
                app:tint="?attr/colorOnSurfaceVariant" />

            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/fab_my_location"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:contentDescription="@string/map_my_location"
                android:src="@drawable/ic_my_location"
                app:shapeAppearanceOverlay="@style/ShapeAppearance.AlarmTracker.Circle" />
        </LinearLayout>
    </FrameLayout>

    <!-- Bottom card: what you picked, how far it is, and the one button that matters. -->
    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardCornerRadius="28dp"
        app:cardElevation="12dp"
        app:shapeAppearanceOverlay="@style/ShapeAppearance.AlarmTracker.MapSheet">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="20dp"
            android:paddingTop="14dp"
            android:paddingEnd="20dp"
            android:paddingBottom="24dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical">

                    <!-- Live reverse-geocoded name of whatever the centre pin is currently over. -->
                    <TextView
                        android:id="@+id/centre_label"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:ellipsize="end"
                        android:maxLines="2"
                        android:text="@string/map_pick_hint"
                        android:textAppearance="?attr/textAppearanceTitleMedium"
                        android:textColor="?attr/colorOnSurface"
                        tools:text="Uppal Southend, Sector 49, Gurugram" />

                    <!-- Road distance + drive time for the blue route line. -->
                    <TextView
                        android:id="@+id/route_info"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="4dp"
                        android:drawablePadding="6dp"
                        android:ellipsize="end"
                        android:maxLines="1"
                        android:textAppearance="?attr/textAppearanceBodyMedium"
                        android:textColor="?attr/colorPrimary"
                        android:visibility="gone"
                        app:drawableStartCompat="@drawable/ic_route"
                        app:drawableTint="?attr/colorPrimary"
                        tools:text="≈14 km · about 16 min by road"
                        tools:visibility="visible" />
                </LinearLayout>

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/save_favorite"
                    style="@style/Widget.Material3.Button.IconButton.Filled.Tonal"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="12dp"
                    android:contentDescription="@string/map_save_favorite"
                    app:icon="@drawable/ic_star" />
            </LinearLayout>

            <!-- Why the map is blank and the search does nothing. Tapping it opens the internet panel. -->
            <TextView
                android:id="@+id/offline_note"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:background="?attr/selectableItemBackground"
                android:drawablePadding="6dp"
                android:paddingVertical="4dp"
                android:text="@string/net_map_offline_note"
                android:textAppearance="?attr/textAppearanceBodySmall"
                android:textColor="?attr/colorTertiary"
                android:visibility="gone"
                app:drawableStartCompat="@drawable/ic_no_internet"
                app:drawableTint="?attr/colorTertiary"
                tools:visibility="visible" />

            <!-- One tap opens the alert-ring controls; the value stays visible either way. -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/details_toggle"
                style="@style/Widget.Material3.Button.TextButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:gravity="start|center_vertical"
                android:insetLeft="0dp"
                android:insetRight="0dp"
                android:paddingStart="4dp"
                android:paddingEnd="4dp"
                android:textAlignment="viewStart"
                app:icon="@drawable/ic_expand_more"
                app:iconGravity="end"
                tools:text="Alert ring · ≈800 m" />

            <LinearLayout
                android:id="@+id/details_content"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:visibility="gone"
                tools:visibility="visible">

                <!-- The ring in the two terms that matter: how close, and how much warning. -->
                <TextView
                    android:id="@+id/radius_value"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textAppearance="?attr/textAppearanceBodySmall"
                    android:textColor="?attr/colorOnSurfaceVariant"
                    tools:text="Alert me within ≈800 m · about 2 min before I arrive" />

                <!-- Common distances in one tap; the slider is for anything in between. -->
                <com.google.android.material.chip.ChipGroup
                    android:id="@+id/radius_chips"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    app:singleLine="true" />

                <com.google.android.material.slider.Slider
                    android:id="@+id/radius_slider"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:contentDescription="@string/map_radius_label"
                    android:stepSize="50"
                    android:value="200"
                    android:valueFrom="150"
                    android:valueTo="10000"
                    app:tickVisible="false" />

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal">

                    <TextView
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:text="@string/map_radius_min"
                        android:textAppearance="?attr/textAppearanceLabelSmall"
                        android:textColor="?attr/colorOnSurfaceVariant" />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/map_radius_max"
                        android:textAppearance="?attr/textAppearanceLabelSmall"
                        android:textColor="?attr/colorOnSurfaceVariant" />
                </LinearLayout>

                <!-- How good the current fix is, so a bad one is visible instead of misleading. -->
                <TextView
                    android:id="@+id/location_accuracy"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="10dp"
                    android:ellipsize="end"
                    android:maxLines="2"
                    android:textAppearance="?attr/textAppearanceBodySmall"
                    android:textColor="?attr/colorOnSurfaceVariant"
                    android:visibility="gone"
                    tools:text="Your location is accurate to about ≈16 m"
                    tools:visibility="visible" />
            </LinearLayout>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/use_location"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:minHeight="56dp"
                android:text="@string/map_pick_use"
                app:icon="@drawable/ic_check" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>
</LinearLayout>
